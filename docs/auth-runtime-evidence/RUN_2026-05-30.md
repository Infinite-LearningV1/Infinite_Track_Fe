# Auth Runtime Evidence — 2026-05-30

## Scope
Evidence for INF-146 Web FE auth/session alignment with INF-145 backend refresh-session contract.

## Sanitization
- No real email, password, token, cookie, or user PII is recorded here.
- Evidence below uses test fixtures and sanitized scenario names.

## Automated evidence collected

### Backend auth code classification
Command:

```text
node --test tests/auth-session-runtime-codes.test.js
```

Result: pass 4 / fail 0.

Covered codes:
- `AUTH_ACCESS_TOKEN_EXPIRED` -> refreshable `access_token_expired`.
- `AUTH_REFRESH_TOKEN_INVALID` -> non-refreshable `refresh_invalid`.
- `AUTH_REFRESH_TOKEN_REVOKED` -> non-refreshable `refresh_invalid`.
- `AUTH_SESSION_INACTIVE` -> non-refreshable `inactivity_expired`.

### Required auth consumer header
Command:

```text
node --test tests/auth-x-client-type-header.test.js
```

Result: pass 5 / fail 0.

Covered calls:
- auth client type is locked to `web`.
- login sends `X-Client-Type: web`.
- refresh sends `X-Client-Type: web`.
- `/auth/me` sends `X-Client-Type: web`.
- service-level bootstrap resolver uses the actual auth service path `GET /api/auth/me -> POST /api/auth/refresh -> GET /api/auth/me` after `AUTH_ACCESS_TOKEN_EXPIRED`.

### Forced reauth notice behavior
Command:

```text
node --test tests/auth-redirect-notice.test.js
```

Result: pass 3 / fail 0.

Covered behavior:
- `AUTH_SESSION_INACTIVE` produces a 6s inactivity notice.
- invalid refresh produces a 6s invalid-session notice.
- sign-in page honors the inactivity 6s minimum timeout.

### Redirect-after-login preservation
Command:

```text
node --test tests/auth-redirect-after-login.test.js
```

Result: pass 5 / fail 0.

Covered behavior:
- forced reauth preserves current same-origin relative path before sign-in redirect.
- cross-origin `redirectAfterLogin` values are ignored.
- current-tab stored redirect targets take precedence over stale shared redirect targets.
- stored redirect target RBAC is checked against the fresh login user, not stale auth-store state.
- stored redirect target is skipped when the fresh-user RBAC helper is unavailable.

### Cross-tab auth sync
Command:

```text
node --test tests/auth-cross-tab-sync.test.js
```

Result: pass 5 / fail 0.

Covered behavior:
- auth storage clear reloads protected pages.
- auth storage clear does not reload public pages.
- `BroadcastChannel("auth-session")` auth-cleared message reloads protected pages.
- `BroadcastChannel` constructor failure does not break storage-event sync.
- auth-cleared broadcast posts the expected message.

### Integrated auth runtime suite
Command:

```text
npm run test:auth-runtime
```

Result: pass 36 / fail 0.

Covered behavior includes classifier mapping, semantic access-token expiry classification, single-flight refresh, protected request replay, direct and replayed non-refreshable protected response forced reauth, bootstrap verification states, token-only session hint survival, canonical/legacy storage cleanup, cleanup failure visibility in auth store/forced reauth/logout paths, forced reauth redirect continuity when redirect notice persistence fails, redirect preservation, semantic `/auth/me` and refresh validation, semantic missing-session denial classification, stale bootstrap fallback after fresh login, auth-store inactivity forced reauth with canonical notice, cookie-based protected request config with stripped caller/resolver `Authorization` headers, auth-store initialization preserving sign-in redirect session artifacts, and manual logout cleanup-failure redirect with warning notice.

### Combined targeted auth regression sweep
Command:

```text
node --test tests/auth-session-runtime.test.js tests/auth-session-runtime-codes.test.js tests/auth-x-client-type-header.test.js tests/auth-redirect-notice.test.js tests/auth-redirect-after-login.test.js tests/auth-cross-tab-sync.test.js
```

Result: pass 58 / fail 0.

### Build verification
Command:

```text
npm run build
```

Result: pass, `BUILD_EXIT_CODE=0`.

### Lint availability
Command:

```text
npm run lint
```

Result: available; exits 1 because Prettier reports repo-wide formatting warnings in 187 files. This auth/session follow-up does not run broad formatting because that would expand the PR beyond INF-146.

## Browser runtime smoke collected

### Protected bootstrap refresh recovery
Harness:

```text
Webpack dev server: http://127.0.0.1:3318/index.html
/api proxy target: http://127.0.0.1:3118
Scenario: cached session hint, first /auth/me returns AUTH_ACCESS_TOKEN_EXPIRED, refresh succeeds, second /auth/me succeeds
```

Result: pass.

Observed auth sequence:

```text
GET /api/auth/me
POST /api/auth/refresh
GET /api/auth/me
```

Observed auth-sensitive headers:
- all observed auth/bootstrap calls sent `X-Client-Type: web`.
- no observed auth/bootstrap call sent an `Authorization` header.

Observed final browser auth state:

```json
{
  "state": "authenticated",
  "ok": true
}
```

## Live-safe backend smoke collected

### Backend health readiness
Command:

```text
GET http://localhost:3005/health
```

Result: pass. Backend returned `status: "OK"` and `ready: true`.

### Refresh without usable refresh session
Commands:

```text
POST http://localhost:3005/api/auth/refresh
X-Client-Type: web
Body: { "refresh_token": "invalid.test.token" }
```

```text
POST http://localhost:3005/api/auth/refresh
X-Client-Type: web
Body: {}
```

Result: pass. Both responses returned sanitized invalid-refresh semantics:

```json
{
  "success": false,
  "code": "AUTH_REFRESH_TOKEN_INVALID",
  "message": "Refresh token invalid"
}
```

## Runtime smoke matrix

| Scenario | Current evidence | Status |
| --- | --- | --- |
| Cached protected bootstrap -> access expired -> refresh success -> continue | Browser runtime smoke proves protected bootstrap recovery sequence `GET /api/auth/me -> POST /api/auth/refresh -> GET /api/auth/me`, with `X-Client-Type: web` and no `Authorization` header. | Verified by browser smoke |
| Refresh denied `AUTH_REFRESH_TOKEN_INVALID` -> forced reauth + notice | Automated forced-reauth notice coverage exists; live-safe backend smoke confirms missing/malformed refresh returns `AUTH_REFRESH_TOKEN_INVALID`. | Partially verified by test and safe smoke |
| Refresh denied `AUTH_REFRESH_TOKEN_REVOKED` -> forced reauth + notice | Automated classifier maps revoked to `refresh_invalid`; live revoked fixture not captured. | Partially verified by test |
| `AUTH_SESSION_INACTIVE` after 48h -> inactivity notice + sign-in redirect | Automated notice coverage exists. | Partially verified by test |
| Offline / 5xx during refresh -> no forced logout, transient alert | Existing runtime classifier separates transport/server; live offline/5xx smoke not captured in this run. | Needs runtime verification |
| Concurrent 3 protected expired requests -> one refresh only | Existing single-flight helper retained; dedicated live concurrency smoke not captured in this run. | Needs runtime verification |

## Known verification gap
Live backend smoke evidence still requires controlled backend fixtures for revoked refresh tokens, inactivity timeout, server failure, and concurrent protected requests. Browser protected-bootstrap refresh recovery should be re-run in an environment with a usable browser after the latest follow-up; direct `/auth/me` probes are not equivalent to the bootstrap recovery path. This file does not claim the remaining live scenarios passed until fixtures are run.
