# Auth Runtime Evidence — 2026-06-21

## Scope

Evidence for Family A hard-deny dashboard access and truthful post-login redirect behavior.

## Sanitization

- No real email, password, token, cookie, or user PII is recorded here.
- Browser evidence uses a safe local stub auth backend with sanitized role fixtures so the Web FE flow can be exercised without leaking credentials into logs or transcripts.
- Live credentialed backend verification for these exact scenarios still REQUIRES REPO VERIFICATION.

## Environment

- App runtime: `npm start` via webpack dev server at `http://127.0.0.1:3000`
- Safe auth runtime used for browser verification: local stub backend at `http://127.0.0.1:3105`
- Verification mode: browser-runtime UI/auth orchestration validation for Web FE, not backend-contract redefinition

## Scenario: Admin clean login -> dashboard

- Before storage snapshot:
  - URL: `http://127.0.0.1:3000/signin.html`
  - `localStorage`: `{}`
  - `sessionStorage`: `{}`
- Login result: `success: true`, role `Admin`
- Final URL: `http://127.0.0.1:3000/index.html`
- After storage snapshot:
  - `localStorage.authToken`: `sanitized-token`
  - `localStorage.userData.role_name`: `Admin`
  - `sessionStorage.redirectAfterLogin`: `null`
  - `document.body.dataset.authBootstrap`: `authenticated`
  - `document.body.dataset.accessBoundary`: `null`
- Console evidence:
  - `Alpine.js auth store updated with user data`
  - `[Infinite Track] GET current user: /api/auth/me`
  - `Redirecting user with role Admin to appropriate page`
  - `Access granted for role Admin to page /index.html`
- Network evidence:
  - `POST /api/auth/login` -> `200`
  - `GET /api/auth/me` -> `200`
  - `GET /api/auth/me` -> `200`
- Status: PASS

## Scenario: Management clean login -> dashboard

- Before storage snapshot:
  - URL: `http://127.0.0.1:3000/signin.html`
  - `localStorage`: `{}`
  - `sessionStorage`: `{}`
- Login result: `success: true`, role `Management`
- Final URL: `http://127.0.0.1:3000/index.html`
- After storage snapshot:
  - `localStorage.authToken`: `sanitized-token`
  - `localStorage.userData.role_name`: `Management`
  - `sessionStorage.redirectAfterLogin`: `null`
  - `document.body.dataset.authBootstrap`: `authenticated`
  - `document.body.dataset.accessBoundary`: `null`
- Console evidence:
  - `Redirecting user with role Management to appropriate page`
  - `[Infinite Track] GET current user: /api/auth/me`
  - `Access granted for role Management to page /index.html`
- Network evidence:
  - `POST /api/auth/login` -> `200`
  - pre-login bootstrap `GET /api/auth/me` -> `401`
  - post-login `GET /api/auth/me` -> `200`
  - dashboard bootstrap `GET /api/auth/me` -> `200`
- Status: PASS

## Scenario: Employee dashboard attempt -> hard deny, no silent redirect to /profile.html

- Before storage snapshot:
  - URL: `http://127.0.0.1:3000/signin.html`
  - `localStorage`: `{}`
  - `sessionStorage`: `{}`
- Login result: `success: true`, role `Employee`
- Post-login truthful role landing before dashboard attempt: `http://127.0.0.1:3000/profile.html`
- Dashboard attempt: navigate authenticated Employee session to `http://127.0.0.1:3000/index.html`
- After dashboard-attempt snapshot:
  - Final URL remains `http://127.0.0.1:3000/index.html`
  - `localStorage.userData.role_name`: `Employee`
  - `sessionStorage.redirectAfterLogin`: `null`
  - `document.body.dataset.authBootstrap`: `authenticated`
  - `document.body.dataset.accessBoundary`: `denied`
  - Deny modal text contains `Akses Ditolak` and `role "Employee" tidak memiliki akses ke halaman dashboard`
- Console evidence:
  - `Auth guard checking page: /index.html`
  - `Checking role-based access for page: /index.html`
  - `User role: Employee`
  - `Access denied for role Employee to page /index.html`
- Network evidence:
  - `POST /api/auth/login` -> `200`
  - pre-login bootstrap `GET /api/auth/me` -> `401`
  - post-login/profile bootstrap `GET /api/auth/me` -> `200`
  - dashboard-attempt bootstrap `GET /api/auth/me` -> `200`
  - no network evidence of a silent redirect back to `/profile.html` after the `/index.html` attempt
- Status: PASS

## Scenario: Internship dashboard denial via browser RBAC harness -> hard deny, no silent redirect to /profile.html

- Reason for separate capture: review follow-up required explicit `Internship` runtime/browser deny evidence, not only Employee evidence plus unit-test coverage.
- Harness path: DevTools browser session on `http://127.0.0.1:3000/signin.html`, sanitized `Internship` session injected into browser storage and Alpine auth store, then `history.pushState({}, '', '/index.html')` and the runtime `window.RoleBasedAccess.initRoleBasedAccess()` entry point were invoked.
- Runtime/browser snapshot:
  - Final browser URL: `http://127.0.0.1:3000/index.html`
  - `localStorage.authToken`: `sanitized-token-internship`
  - `localStorage.userData.role_name`: `Internship`
  - `sessionStorage.redirectAfterLogin`: `null`
  - `document.body.dataset.accessBoundary`: `denied`
  - Deny modal text contains `Akses Ditolak` and `role "Internship" tidak memiliki akses ke halaman dashboard`
  - Primary action text: `Buka Halaman Sesuai Role`
  - No silent browser URL change to `/profile.html` occurred during the deny assertion.
- Console evidence:
  - `User data set in auth store: [object Object]`
  - `Checking role-based access for page: /index.html`
  - `User role: Internship`
  - `Access denied for role Internship to page /index.html`
- Network evidence:
  - No additional auth/network request was required for this follow-up probe because the browser harness exercised the already-loaded runtime RBAC entry point with sanitized verified-user state.
  - Full page-startup Internship proof through a live credentialed backend remains `REQUIRES REPO VERIFICATION` because this session does not have approved non-leaking real `Internship` credentials.
- Status: PASS for runtime/browser RBAC deny evidence; live credentialed backend startup path remains `REQUIRES REPO VERIFICATION`.

## Scenario: stale redirectAfterLogin=/profile.html for Admin -> ignored

- Before storage snapshot:
  - URL: `http://127.0.0.1:3000/signin.html`
  - `localStorage`: `{}`
  - `sessionStorage.redirectAfterLogin`: `/profile.html?from=stale-login`
- Login result: `success: true`, role `Admin`
- Final URL: `http://127.0.0.1:3000/index.html`
- After storage snapshot:
  - `localStorage.authToken`: `sanitized-token`
  - `localStorage.userData.role_name`: `Admin`
  - `sessionStorage.redirectAfterLogin`: `null`
  - `document.body.dataset.authBootstrap`: `authenticated`
  - `document.body.dataset.accessBoundary`: `null`
- Console evidence:
  - `User role Admin doesn't have access to /profile.html, redirecting based on role`
  - `[Infinite Track] GET current user: /api/auth/me`
  - `Access granted for role Admin to page /index.html`
- Network evidence:
  - `POST /api/auth/login` -> `200`
  - pre-login bootstrap `GET /api/auth/me` -> `401`
  - post-login `GET /api/auth/me` -> `200`
  - dashboard bootstrap `GET /api/auth/me` -> `200`
- Status: PASS

## Verification summary

- Focused auth-flow regression sweep: PASS
- `npm run test:auth-runtime`: PASS
- `npm run build`: PASS
- `npm run lint`: REPO-WIDE PRETTIER DRIFT from the existing baseline; the failures did not implicate the newly touched Task 3 docs/evidence files after the follow-up targeted Prettier check.
- Follow-up docs formatting check: `npx prettier --check docs/auth-runtime-evidence/RUN_2026-06-21.md .superpowers/sdd/task-3-report.md` -> PASS.
- Explicit Internship runtime/browser deny follow-up: PASS via browser RBAC harness; live credentialed backend/browser verification for Admin, Management, and Employee/Internship accounts remains REQUIRES REPO VERIFICATION.
