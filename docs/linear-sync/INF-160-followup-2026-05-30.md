# INF-160 Follow-up Drafts (2026-05-30)

Paste-ready update drafts for Linear sync.

## INF-160

Contract adoption is aligned to three canonical cockpit authority surfaces: `/summary/dashboard-analytics`, `/attendance/today-locations`, and `/summary`. `/api/summary` is explicitly non-canonical for this cockpit flow, and `/api/summary/dashboard-map` is not used. Fuzzy AHP discipline is enforced as snapshot from analytics with detail lazy-loaded from `/analysis/fuzzy-ahp`. ADR-007 shell and evidence run shell were added for traceability and verification execution.

## INF-159

Dashboard analytics alignment remains tied to canonical `/summary/dashboard-analytics` contract for cockpit panels. No new alias authority was introduced. Follow-up should focus on runtime payload verification and ensuring panel states remain conservative when canonical analytics fields are missing/incomplete.

## INF-128

Reporting/export authority remains on canonical `/summary` (selected-period report path). This follow-up keeps cockpit and reporting boundaries explicit to prevent summary alias drift and avoid cross-surface truth conflicts.

## INF-166

Today live-map authority remains canonical on `/attendance/today-locations`; `/api/summary/dashboard-map` is not part of active flow. Follow-up validation should confirm coordinate quality and conservative fallback behavior when rows are incomplete.

## INF-155 / INF-171

Fuzzy AHP contract discipline remains: snapshot from analytics payload and detail from `/analysis/fuzzy-ahp` lazy-load path. Follow-up should verify consistency between snapshot and detail payloads across selected periods/custom ranges and ensure no UI state implies backend truth when detail is unavailable.
