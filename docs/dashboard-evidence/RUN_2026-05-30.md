# Dashboard Evidence Run Shell - 2026-05-30

## Scope

INF-160 contract-adoption evidence for cockpit authority surfaces and fuzzy-ahp discipline checks.

## Environment

- Base URL: `<REQUIRES_SAFE_RUNTIME_BASE_URL>`
- Auth token source: `<REQUIRES_SAFE_AUTH_TOKEN_OR_APPROVED_LOGIN_FLOW>`
- Operator: Claude Code implementer subagent
- Timestamp (UTC): `2026-05-30T14:52:06Z`

## Verification Commands

### Prior lint investigation: `npm run lint`

- Result: BLOCKED / not green
- Blocking detail: The lint script runs, but repo-wide formatting drift and non-scope HTML parse errors, including `src/partials/common/header.html`, keep lint non-green.
- Task decision: User explicitly chose to continue remaining verification and report lint as blocked by broader repo drift.

### 1) `node --test tests/dashboard-analytics-service.test.js`

- Result: PASS
- Exit code: 0
- Evidence summary: 6 tests passed, 0 failed.
- Notes: Node emitted `MODULE_TYPELESS_PACKAGE_JSON` warning only.

### 2) `node --test tests/today-locations-service.test.js`

- Result: PASS
- Exit code: 0
- Evidence summary: 4 tests passed, 0 failed.
- Notes: Node emitted `MODULE_TYPELESS_PACKAGE_JSON` warning only.

### 3) `node --test tests/fuzzy-ahp-service.test.js`

- Result: PASS
- Exit code: 0
- Evidence summary: 5 tests passed, 0 failed.
- Notes: Node emitted `MODULE_TYPELESS_PACKAGE_JSON` warning only.

### 4) `node --test src/js/services/reportService.test.js`

- Result: PASS
- Exit code: 0
- Evidence summary: 14 tests passed, 0 failed.
- Notes: Expected negative-path console errors appeared for injected unavailable services; Node emitted `MODULE_TYPELESS_PACKAGE_JSON` warning only.

### 5) `node --test tests/dashboard-range-state.test.js`

- Result: PASS
- Exit code: 0
- Evidence summary: 8 tests passed, 0 failed.
- Notes: Node emitted `MODULE_TYPELESS_PACKAGE_JSON` warning only.

### 6) `node --test tests/dashboard-map-renderer.test.js`

- Result: PASS
- Exit code: 0
- Evidence summary: 4 tests passed, 0 failed.
- Notes: Node emitted `MODULE_TYPELESS_PACKAGE_JSON` warning only.

### 7) `npm run build`

- Result: PASS
- Exit code: 0
- Evidence summary: Production webpack command completed successfully.

```text
> infinitetrack-free@2.0.1 build
> cross-env NODE_ENV=production webpack --config webpack.config.js
```

### 8) `npm run test:auth-runtime`

- Result: PASS
- Exit code: 0
- Evidence summary: 18 tests passed, 0 failed.
- Notes: Node emitted `MODULE_TYPELESS_PACKAGE_JSON` warning only.

## Smoke Evidence

- Result: BLOCKED
- Blocking detail: Authenticated smoke requests require a safe runtime base URL and safe auth token or approved non-leaking login flow. Attempting to inspect token environment variables was denied as credential exploration, and attempting to run a command embedding a real password/token flow was denied because it would leak credentials into persistent logs/transcript.
- Locked-field status: NOT VERIFIED because smoke responses were not safely obtainable in this subagent session.
- Required locked fields still requiring runtime verification where expected:
  - `executive_kpis`
  - `mode_mix.percentages`
  - `historical_trend.points`
  - `fuzzy_ahp_snapshot`
  - `geofence_evidence_context`

---

## 1) dashboard-analytics period=30d

### Command

```bash
curl -sS -H "Authorization: Bearer <TOKEN>" \
  "<BASE_URL>/summary/dashboard-analytics?period=30d"
```

### Expected

- HTTP 200
- Envelope/data shape consistent with cockpit analytics authority
- Includes locked fields where expected: `executive_kpis`, `mode_mix.percentages`, `historical_trend.points`, `fuzzy_ahp_snapshot`, `geofence_evidence_context`

### Actual

- Status: BLOCKED
- Sanitized output: `<not captured; safe auth/base-url input required>`
- Notes: Do not infer contract success without live response body field checks.

## 2) dashboard-analytics custom

### Command

```bash
curl -sS -H "Authorization: Bearer <TOKEN>" \
  "<BASE_URL>/summary/dashboard-analytics?period=custom&from=<YYYY-MM-DD>&to=<YYYY-MM-DD>"
```

### Expected

- HTTP 200
- Custom range respected
- No fallback to non-canonical `/api/summary` alias
- Includes locked fields where expected: `executive_kpis`, `mode_mix.percentages`, `historical_trend.points`, `fuzzy_ahp_snapshot`, `geofence_evidence_context`

### Actual

- Status: BLOCKED
- Sanitized output: `<not captured; safe auth/base-url input required>`
- Notes: Do not infer contract success without live response body field checks.

## 3) today-locations limit=5

### Command

```bash
curl -sS -H "Authorization: Bearer <TOKEN>" \
  "<BASE_URL>/attendance/today-locations?limit=5"
```

### Expected

- HTTP 200
- Up to 5 rows
- Coordinate-bearing rows usable for live-map authority

### Actual

- Status: BLOCKED
- Sanitized output: `<not captured; safe auth/base-url input required>`
- Notes: Coordinate quality remains runtime-unverified.

## 4) reports period=30d page=1 limit=10

### Command

```bash
curl -sS -H "Authorization: Bearer <TOKEN>" \
  "<BASE_URL>/summary?period=30d&page=1&limit=10"
```

### Expected

- HTTP 200
- Report/export authority payload with pagination completeness metadata

### Actual

- Status: BLOCKED
- Sanitized output: `<not captured; safe auth/base-url input required>`
- Notes: Report/export runtime payload remains unverified.

## 5) fuzzy-ahp discipline monthly

### Command

```bash
curl -sS -H "Authorization: Bearer <TOKEN>" \
  "<BASE_URL>/analysis/fuzzy-ahp?type=discipline&period=monthly"
```

### Expected

- HTTP 200
- Detail payload available for lazy-load flow
- Detail consistent with analytics snapshot expectations

### Actual

- Status: BLOCKED
- Sanitized output: `<not captured; safe auth/base-url input required>`
- Notes: Fuzzy AHP analytics snapshot vs detail consistency remains runtime-unverified.

---

## Contract Assertion Checklist

- [x] Canonical analytics endpoint used in service tests: `/summary/dashboard-analytics`
- [x] Canonical today-locations endpoint used in service tests: `/attendance/today-locations`
- [x] Canonical reports endpoint used in service tests: `/summary`
- [x] `/api/summary` alias not treated as canonical in cockpit service tests
- [x] `/api/summary/dashboard-map` not used by focused verification commands
- [ ] Fuzzy AHP snapshot-from-analytics and detail-from-`/analysis/fuzzy-ahp` runtime discipline holds

## Overall Result

- Targeted tests/build/auth-runtime: PASS, except lint remains blocked by broader repo drift from prior investigation.
- Runtime smoke evidence: BLOCKED pending safe auth/base URL inputs or an approved non-leaking login flow.
